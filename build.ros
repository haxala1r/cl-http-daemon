#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '(:usocket :bordeaux-threads) :silent t)
  )

(defvar *sources* (list
		    "src/package.lisp"
		    "src/string.lisp"
		    "src/stream.lisp"
		    "src/arguments.lisp"
		    "src/main.lisp"))
(loop for i in *sources* do
      (load (compile-file (probe-file (pathname i)))))
(defun main (&rest argv)
  (http-daemon:main))
;;; vim: set ft=lisp lisp:
